# Local `npm run dev` only. Production .env is written on the VPS by
# GitHub Actions from repository secrets — do not copy this file to the server.
#
# Lead delivery. Telegram is the primary channel and is required.
# Token: @BotFather → /newbot
# chat_id: send the bot any message, then open
#   https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
